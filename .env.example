# =============================================================================
# Plot Your Path — Environment Configuration
# Copy this file to .env and fill in real values. .env is gitignored.
# =============================================================================

# -----------------------------------------------------------------------------
# LLM Provider — OpenRouter (recommended: one account, many models)
# Get your key at https://openrouter.ai/keys
# -----------------------------------------------------------------------------
OPENROUTER_API_KEY=sk-or-your-key-here

# Alternative providers (change api_key_env in config/llm.json to use these)
# OPENAI_API_KEY=sk-your-openai-key-here
# ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here

# -----------------------------------------------------------------------------
# Data Root — where the database and all job files are stored (outside repo)
# Use an absolute path. ~ is expanded automatically.
# -----------------------------------------------------------------------------
DATA_ROOT=~/Documents/plot_your_path

# -----------------------------------------------------------------------------
# Backend Server (optional — only needed when running the FastAPI server)
# -----------------------------------------------------------------------------
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# -----------------------------------------------------------------------------
# Frontend (optional — only needed when running the Next.js dev server)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_API_URL=http://localhost:8000
